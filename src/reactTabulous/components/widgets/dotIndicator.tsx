import styled from '@emotion/styled';

export const DotIndicator = styled.div({
  width: 8,
  height: 8,
  position: 'absolute',
  right: 4,
  top: 0,
  borderRadius: '50%',
  backgroundColor: 'currentColor'
});
